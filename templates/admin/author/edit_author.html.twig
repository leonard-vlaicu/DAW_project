{% extends 'admin/index.html.twig' %}

{% block title %}Edit author{% endblock %}

{% block manage_content %}
    {% for error in app.flashes('error') %}
        <div class="alert alert-error alert-soft mb-10">{{ error }}</div>
    {% endfor %}

    {% for message in app.flashes('success') %}
        <div class="alert alert-success alert-soft mb-10">{{ message }}</div>
    {% endfor %}

    <h1 class="text-2xl">Edit existing author</h1>

    <div class="">
        <div class="">
            <div class="flex justify-start">
                {{ form_start(editAuthorForm) }}

                <div class="form-control mt-4">
                    <label class="input">
                        First name:
                        {{ form_widget(editAuthorForm.firstName,
                            {'attr': {'placeholder': 'Enter author first name'}}) }}
                    </label>
                    <div class="text-error mt-1">
                        {{ form_errors(editAuthorForm.firstName) }}
                    </div>
                </div>

                <div class="form-control mt-4">
                    <label class="input">
                        First name:
                        {{ form_widget(editAuthorForm.lastName,
                            {'attr': {'placeholder': 'Enter author last name'}}) }}
                    </label>
                    <div class="text-error mt-1">
                        {{ form_errors(editAuthorForm.lastName) }}
                    </div>
                </div>

                <div class="mt-4">
                    <button type="submit" class="btn btn-primary">Update</button>
                    <a href="{{ path('app_admin_authors') }}" class="btn btn-accent">Cancel</a>
                </div>

                {{ form_end(editAuthorForm) }}
            </div>
        </div>
    </div>

{% endblock %}
